export enum ENVS {
  PROD = 'prod',
  STAGE = 'stage',
  DEV = 'dev',
  LOCAL = 'local',
  TESTS = 'tests',
}

export enum DELETE_TYPE {
  SOFT = 'soft',
  HARD = 'hard',
}

export enum LESSON_CATEGORY {
  SINGLE = 'single',
  GROUP = 'group',
}
